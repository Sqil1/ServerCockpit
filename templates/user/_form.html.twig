{{ form_start(form, {'attr': {'class': 'user-form-inner'}}) }}
    {{ form_widget(form) }}
<div class="text-center mt-4">
    <button class="btn btn-primary btn-lg">
        <i class="fas fa-save"></i> {{ button_label|default('Enregistrer') }}
    </button>
</div>
{{ form_end(form) }}